import {defineCliConfig} from 'sanity/cli'

export default defineCliConfig({
  api: {
    projectId: 'cg5jvog9',
    dataset: 'production'
  },
  studioHost: 'karpuchinagallery',
  autoUpdates: true,
})
